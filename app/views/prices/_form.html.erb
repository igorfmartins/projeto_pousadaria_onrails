<%= form_with(model: [@room, @price], local: true) do |f| %>
  <%= f.hidden_field :room_id, value: @room.id %>
  <%= f.date_field :start_date %>
  <%= f.date_field :end_date %>
  <%= f.text_field :daily_rate %>
  <%= f.submit 'Salvar' %>
<% end %>
